
import React from 'react';
import { expect } from 'chai';
import { shallow, mount, render } from 'enzyme';


describe("First Test", function() {
  it("Enzyme working in terminal", function() {
    expect(true).to.equal(true);
  });
});
